# Environnement complet, dependances transitives comprises.
#
# requirements.txt fige les dependances DIRECTES ; ce fichier fige aussi
# celles qu'elles entrainent (urllib3, certifi, soupsieve...). C'est lui qui
# garantit qu'une execution sur GitHub Actions dans six mois installe
# exactement le meme environnement qu'aujourd'hui.
#
# Installation reproductible :
#     pip install -r requirements.lock
#
# Regeneration apres une montee de version :
#     pip install -r requirements.txt && pip freeze > requirements.lock
#
# Genere le 2026-07-28 avec Python 3.11.

beautifulsoup4==4.15.0
certifi==2026.2.25
charset-normalizer==3.4.6
idna==3.11
python-dotenv==1.2.2
requests==2.33.1
soupsieve==2.9.1
urllib3==2.6.3
